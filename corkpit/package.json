{
  "name": "multi-agent-cockpit",
  "version": "0.2.0",
  "description": "Multi-level agent cockpit: adapter registry, canonical data model, local visualization",
  "type": "module",
  "bin": {
    "cockpit": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js serve",
    "dev": "tsc && node dist/cli.js serve"
  },
  "dependencies": {
    "smol-toml": "^1.3.1",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}